name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: 4.6.5
  flutter_modular: 5.0.2
  get_storage: 2.0.3
  core:
    path: 'packages/core'
  buzz_ui:
    path: 'packages/buzz_ui'
  buzz:
    path: '../'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
